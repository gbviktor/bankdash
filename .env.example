# Influx init (docker image supports init env vars) :contentReference[oaicite:0]{index=0}
INFLUXDB_ADMIN_USER=admin
INFLUXDB_ADMIN_PASSWORD=change_me_please
INFLUXDB_ORG=bankdash
INFLUXDB_BUCKET=bankdash
INFLUXDB_TOKEN=change_me_to_a_long_token

# Grafana admin
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin

# Backend
BACKEND_PORT=8080
META_DB_PATH=/data/meta.db

# default tenant (we’ll extend later)
DEFAULT_TENANT_ID=default
